# Copy this file to .env and adjust as needed.
# Local dev: the FastAPI backend runs on http://localhost:8000
# Production: set this to your deployed backend URL (and set it in Vercel's
# Environment Variables, since .env is git-ignored and won't be deployed).
VITE_API_URL=http://localhost:8000